DATABASE_URL="postgresql://leafandcup:leafandcup@localhost:5433/leafandcup?schema=public"

# Generate with: openssl rand -base64 32
AUTH_SECRET=""

# https://resend.com/api-keys — used for password reset emails
RESEND_API_KEY=""

# https://console.cloud.google.com/apis/credentials — OAuth client (Web application)
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# https://dashboard.stripe.com/test/apikeys — test mode keys
STRIPE_SECRET_KEY=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
# From `stripe listen` (local dev) or the webhook endpoint in the Stripe dashboard
STRIPE_WEBHOOK_SECRET=""
